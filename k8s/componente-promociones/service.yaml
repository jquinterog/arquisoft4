apiVersion: v1
kind: Service
metadata:
  name: componente-promociones
  namespace: arquisoft-local
spec:
  type: ClusterIP
  selector:
    app: componente-promociones
  ports:
    - name: http
      port: 8000
      targetPort: 8000
