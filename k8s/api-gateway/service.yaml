apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: arquisoft-local
spec:
  type: NodePort
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30080
