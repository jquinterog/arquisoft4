apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: voting-pod-restart-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: voting-pod-restart
subjects:
  - kind: ServiceAccount
    name: voting-sa
    namespace: arquisoft-local
